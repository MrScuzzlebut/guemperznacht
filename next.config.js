/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // NEXT_PUBLIC_* variables are automatically exposed by Next.js
  // No need to explicitly define them in env
}

module.exports = nextConfig
